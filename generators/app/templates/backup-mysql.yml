version: '2'
services:
    mysql-backup:
      user: "0"
      image: deitch/mysql-backup
      environment:
       - DB_DUMP_TARGET=/backup
       - DB_USER=root
       - DB_DUMP_BEGIN=+0
      volumes:
       - ./backup:/backup
      restart: always
